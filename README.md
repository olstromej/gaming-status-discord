# Gaming Status → Discord

Checks Steam (Web API + Store) and PlayStation status page. Sends alerts to a Discord channel via webhook.

- Runs every 10 minutes on GitHub Actions
- Edit checks in `monitor.py`
